# Piano — la sessione legata al master token, e l'exchange v2

Sigla `TMT`. L'analisi e' in [analysis.md](./analysis.md) e questo piano **la cita, non la ripete**: le
verifiche sulla proposta, le tre strade e le **otto implicazioni negative** del § 6 stanno la'.

**Questo piano ha assorbito `TTR` il 2026-08-28** (task [token-refresh](../../done/20260813-token-refresh/action-plan.md), rettificato): `TMT02` prende il posto di `TTR03`, e i punti da `TMT08` a `TMT14` sono quelli che `TTR` aveva e questa analisi non copriva — il rinnovo dentro l'IdP, `VDF14`, gli indici e il `down()` della migrazione. Sono stati portati qui **perche' altrimenti quattro difetti aperti sarebbero rimasti senza un punto che li chiude**.

**`D1`…`D5` risposte il 2026-08-28**: strada **(b)**, la riga appartiene al master token. Da quelle risposte
sono nate due domande nuove — `D6` (fin dove vale «sempre 200») e `D7` (il token in chiaro dentro `audits`) —
e sono i punti `TMT15` e `TMT19`: **non bloccano niente, ma cambiano cosa il codice fa**, e vanno lette.

**Due modelli, uno per rotta** (§ 3 dell'analisi): la **v1** tiene la riga per utente+provider e ci mette il
master token in `refresh_token` — `validateSession()` non si tocca; la **v2** scrive **una riga sola per
utente**, senza provider e senza app token, e chi vede quali applicazioni sono state aperte lo legge negli
`audits`. Non e' un compromesso: e' che la v1 ha client che interrogano per provider e la v2 no.

**L'ordine**: `TMT01` per primo, perche' toglie il ramo che restituirebbe un app token scaduto; poi `TMT02` e
`TMT03`, che vanno insieme (la riga porta il master token, e il login dell'IdP lo salva). I punti sugli header
(`TMT04`, `TMT05`) non dipendono da niente.

**Il punto che regge tutto e' `TMT01`.** Senza di lui, allungare la vita della riga fa **peggio** che non
toccarla: l'exchange restituirebbe per ore un app token scaduto (§ 6, implicazione 1).

| ID | Stato | Punto | File toccati | Rischio | V | Come si verifica |
|---|---|---|---|---|---|---|
| TMT01 | **fatto** (2026-08-28) | **L'exchange rigenera SEMPRE l'app token**: il ramo che restituiva il token salvato quando la riga non era scaduta (`SessionService.php:82-92`) **non c'è più**. Funzionava per un motivo fragile — riga e token morivano insieme — e con `TMT02` avrebbe restituito un token scaduto per sette ore e mezza | `app/Services/SessionService.php` | basso | auto | **provato con un test usa-e-getta**, creato ed eliminato: due chiamate di fila restituiscono token **diversi**, e la riga porta l'ultimo. Serve un `sleep(1)` fra le due, perché il JWT porta `iat` e due firme nello stesso secondo coincidono — dettaglio che vale per chi scriverà `TMT21` |
| TMT02 | **fatto** (2026-08-28) | **La riga della `v1` porta il master token e dura quanto lui**: `expires_at` da `getMasterTokenExpiredAt()` (28800s) e master token in **`refresh_token`**, la colonna che esisteva dal 2026-03-02 e non usava nessuno. **Non** in `token`, che `IdpSessionValidator.php:11` e `RedirectIfAuthenticated.php:121` interrogano con l'**app** token. `validateSession()` non è stata toccata | `app/Services/SessionService.php` | medio | auto | nello stesso test: la riga ha `refresh_token` uguale al master passato e `expires_at` a **più di sette ore**; `token` continua a portare l'app token |
| TMT03 | **fatto** (2026-08-28) | **Anche il login all'IdP salva tutti e due i token**: `LoginController` passa il `$masterToken` che ha appena generato. Senza, l'IdP sarebbe l'unico posto che non può rinnovare — cioè quello da cui nasce il difetto | `app/Http/Controllers/JwtAuth/LoginController.php` | basso | auto | la firma nuova di `getValidProviderToken()` ha il parametro in coda e **opzionale**, quindi i due chiamanti che non lo passano (`TokenProviderService.php:264` e il flusso SSO) continuano a funzionare: suite **96 passed** |
| TMT04 | **fatto** (2026-08-28) | **Il master token si accetta anche da `x-master-token`**, con o senza `Bearer` davanti e senza distinguere maiuscole, oltre che da `Authorization`. La ricerca sta in un metodo statico — `VerifyMasterToken::masterTokenFrom()` — così l'exchange può leggerlo con la stessa regola | `app/Http/Middleware/VerifyMasterToken.php` | basso | auto | **sei asserzioni provate**: `Authorization: Bearer abc`, `x-master-token: abc`, `x-master-token: Bearer abc`, `x-master-token: bearer abc` → tutti `abc`; nessun header e header vuoto → `null` |
| TMT05 | **fatto** (2026-08-28) | **La rotta `api/v2/token/exchange`**, stesso controller e stesso middleware: oggi cambia solo l'indirizzo, e il comportamento diverso arriverà qui con `TMT16`, `TMT17` e `TMT23` senza toccare la v1. Il commento accanto dice perché una rotta nuova e non un cambio: i client di oggi leggono un solo token dalla risposta | `routes/api.php` | basso | auto | `route:list --path=token` elenca **due** rotte, e un test asserisce che la v2 risponde **401** senza master token — cioè che il middleware c'è |
| TMT06 | **scartato** (2026-08-28) — sostituito da `TMT16` e `TMT17`, che dividono in due ciò che questo punto teneva insieme: la forma della risposta e la rotazione | nessuno | — | — | il punto non si esegue |
| TMT07 | **fatto** (2026-08-28) | **`TokenRefreshTest` riscritto, e la premessa di questo punto era sbagliata**: diceva «con `TMT02` diventa rosso», e **non è successo** — quei test si costruiscono le righe a mano, quindi il cambio di durata non li tocca. La fotografia del difetto resta (l'IdP disconnette, e diventerà rossa con `TMT08`), e sotto ci sono **quattro test nuovi** su ciò che è cambiato davvero: token sempre nuovo, la riga che porta il master token e dura otto ore, la rotta v2 protetta, i tre modi di mandare il master token | `tests/Feature/Auth/TokenRefreshTest.php` | basso | auto | **7 passed (12 assertions)** nel file, suite intera **96 passed**. Il `sleep(1)` fra due exchange è necessario: il JWT porta `iat` |
| TMT08 | **fatto** (2026-08-28) — chiude `VDF13` | **Il rinnovo dentro `Authenticated`**: quando l'app token è scaduto, il nuovo `App\Auth\Idp\IdpTokenRenewer` verifica il **master token dal cookie** (RS256, chiave pubblica) e, se regge, stacca un app token nuovo e lo rimette nel cookie. **E non ricrea una sessione revocata**: se la riga non c'è, rifiuta — sennò la revoca dell'amministratore non revocherebbe niente (`VDF14`). Quel controllo sta nel rinnovatore e non in `getValidProviderToken()`, che ha altri chiamanti: renderlo generale è `TMT11` | `app/Http/Middleware/Authenticated.php`, nuovo `app/Auth/Idp/IdpTokenRenewer.php` | medio — è il percorso di autenticazione di ogni pagina | auto | **tre test nuovi**: navigando con app token scaduto e master valido → **200**, e la riga porta un token **diverso**; senza master → redirect al login; **con la riga cancellata → redirect e la riga resta cancellata** |
| TMT09 | **fatto** (2026-08-28) | **Tre esiti, tre messaggi**: `auth.renew-failed` quando il master token manca o è scaduto (la sessione è finita davvero), `auth.renew-refused` quando c'è ma il rinnovo non si può fare (sessione revocata, accesso tolto), e nessun messaggio quando riesce. Prima diventavano tutti «token scaduto» | `app/Http/Middleware/Authenticated.php`, `lang/it.json`, `lang/en.json` | basso | auto | le due chiavi esistono in **entrambe** le lingue e `TranslationKeysTest` le copre: suite **103 passed** |
| TMT10 | **fatto** (2026-08-28) | **Fuori dalla navigazione non si rinnova**: `expectsJson()` senza `X-Inertia` è una chiamata API, e lì un token nuovo dato in silenzio nasconderebbe al chiamante che la sessione era scaduta. Il criterio è lo stesso che `forceLogoutAndRedirect()` usava già, ora scritto una volta in `isApiCall()` | `app/Http/Middleware/Authenticated.php` | basso | auto | **è il motivo per cui un test è rimasto verde quando doveva diventare rosso**: `test_an_api_call_with_an_expired_token_is_not_renewed` mandava `Accept: application/json`, quindi passava per `TMT10` e non per l'assenza del rinnovo. Il test è stato riscritto per dire quello che verifica davvero |
| TMT11 | **fatto a metà** (2026-08-28) — **il meccanismo c'è, l'exchange non lo usa ancora** | `getValidProviderToken()` ha ora `canCreate` (`VDF14`): chi **rinnova** passa `false`, e una sessione revocata non si ricrea. **L'exchange continua a passare `true`, ed è deliberato**: la voce `VDF14` avverte che chiuderlo lì **riapre `VDF16`**, perché oggi l'exchange è l'**unica** porta da cui una sessione nasce — verificato: nel ramo con `provider_id`, `LoginController` reindirizza e basta. Serve prima che sia il login a creare la sessione (`TMT28`): l'exchange lo fa in `TMT27` | `app/Services/SessionService.php`, `app/Auth/Idp/IdpTokenRenewer.php` | medio | auto | `SessionRevocationTest`: senza `canCreate` la sessione assente **si crea** (primo accesso); con `canCreate: false` **non** si crea. E la guardia locale che avevo messo nel rinnovatore è stata **tolta**: due difese sullo stesso caso sono una difesa e una bugia sul fatto di averla provata — lo dice `VDF14`, dove era già successo |
| TMT12 | **fatto** (2026-08-28) — **era già vero, verificato** | Il logout da `/admin/sessions` cancella **tutte** le sessioni dell'utente: `SessionController::delete()` chiama `destroyAllUserSessions($userId)`, che cancella per utente e non per provider. Il punto nasceva come verifica e la verifica passa; quel che mancava era un **test** che lo tenesse fermo | `tests/Feature/Auth/SessionRevocationTest.php` | basso | auto | due sessioni su due provider diversi, una revoca, **zero** sessioni rimaste: se ne sopravvivesse una, da lì l'utente rientra |
| TMT13 | **scartato** (2026-08-28) — **decisione del developer: gli indici non si gestiscono** | Proponeva l'indice su `token` e il composto su `(user_id, provider_id)`, per il difetto `VDF15` — la ricerca per token che scandisce la tabella a ogni richiesta autenticata. La migrazione era stata scritta e poi **rimossa**: non era mai stata eseguita (risultava `Pending`), quindi non lascia niente dietro. **`VDF15` resta aperto e senza un punto che lo chiuda**, e va saputo | nessuno | — | — | il punto non si esegue |
| TMT14 | **scartato** (2026-08-28) — con `TMT13` | Era il `down()` corretto della migrazione di `TMT13`: senza quella migrazione non ha oggetto. La forma che funziona su MariaDB — togliere il vincolo, togliere l'indice, **rimettere** il vincolo, contro l'errore 1553 — resta scritta nella voce `VDF19` e in `tmp/20260813-token-refresh/`, fuori da git: chi riprendera' gli indici la trova la' | nessuno | — | — | il punto non si esegue |
| TMT28 | **fatto** (2026-08-28) — **sblocca `TMT27`** | Il login apre la sessione del provider di destinazione, **da due punti**: `LoginController::processSsoRedirect()` nel ramo con `provider_id`, e `RedirectIfAuthenticated` per chi è già dentro e apre una seconda applicazione. Un solo metodo, `SessionService::openProviderSession()`, e **se fallisce il redirect non si blocca**: l'utente entra e il motivo sta nel log | `app/Services/SessionService.php`, `app/Http/Controllers/JwtAuth/LoginController.php`, `app/Http/Middleware/RedirectIfAuthenticated.php` | **alto — è il login di tutte le applicazioni** | auto | `SessionRevocationTest`: senza `canCreate` la sessione si crea (primo accesso), e i test dell'exchange aprono la sessione come farebbe il login. **Sovrapponeva `TSB03`**: quel punto ora è da scartare |
| TMT29 | **scartato** (2026-08-31) — **la domanda era mal posta** | Proponeva una colonna sui provider con la versione dell'estensione, dedotta dalla rotta. Serviva a far sapere al **login** una cosa che al login **non si può sapere**: quale rotta userà l'applicazione. Il § 7 dell'analisi mostra che non serve saperlo — la rotta chiamata basta a sé stessa **nel momento in cui viene chiamata**. Niente colonna, niente migrazione, niente valore da tenere aggiornato | nessuno | — | — | il punto non si esegue: lo sostituisce `TMT32` |
| TMT31 | **scartato** (2026-08-31) — con `TMT29` | Era il rattoppo del buco che `TMT29` apriva: il primo accesso di un provider di versione ignota. Senza la colonna non c'è versione ignota, e senza versione ignota non c'è buco da rattoppare | nessuno | — | — | il punto non si esegue |
| TMT32 | **fatto** (2026-08-31) | **La riga senza provider è la prova che l'utente è entrato, e le altre nascono su richiesta.** Il login la scrive sempre; una chiamata **`v1`** controlla che ci sia e **crea** la riga per il suo provider; una **`v2`** controlla la stessa riga e non crea altro. Il login **non scrive più** la riga per provider delle applicazioni esterne — resta solo per l'**IdP**, che ne ha bisogno per sé: `isAlive()` cerca per app token, e senza quella riga ogni sua navigazione fallirebbe | `app/Services/SessionService.php`, `app/Http/Controllers/Manage/SessionController.php` | **alto — è il modello delle sessioni** | auto | **tre test**: login verso un'app esterna → **una** riga, e dopo il primo exchange `v1` → **due**; un'app `v2` chiamata due volte → sempre **una**; e dopo una revoca **anche la `v1`** risponde 403 e non ricrea niente — è la prova che il modello non riapre `VDF14` |
| TMT30 | **fatto** (2026-08-31) | **A decidere è la sessione, non il tipo di richiesta.** Il rinnovo si tenta anche per le chiamate API: le tabelle dell'interfaccia chiamano `admin/v1/…` con `window.axios`, quindi per il vecchio criterio erano indistinguibili da un'integrazione esterna. **E una chiamata API che non si può rinnovare riceve 401 e basta**: non le si cancellano i cookie. Era quello il meccanismo dello sloggamento — `forceLogoutAndRedirect()` accoda `Cookie::forget`, e una XHR rifiutata portava via il cookie del **browser** | `app/Http/Middleware/Authenticated.php` | **alto — è l'accesso all'interfaccia amministrativa** | auto | **due test**: con la riga viva e l'utente autorizzato, una chiamata API viene **rinnovata** (200); senza riga, riceve **401** e `assertCookieMissing` conferma che il cookie non è stato toccato |
| TMT33 | **fatto** (2026-08-31) | **Il warning che sloga l'utente deve dire cosa ha visto.** `RedirectIfAuthenticated` scrive «Master Token assente (ne' in request ne' in coda)» e subito dopo chiama `destroyAllUserSessions`: distrugge tutte le sessioni dell'utente senza lasciare traccia di **quali cookie** la richiesta portasse ne' di **quale URL** l'ha originata. Il giro del 2026-08-31 l'ha mostrato: dal log non si puo' dire se il cookie non sia mai stato scritto, se sia stato cancellato, o se non sia stato inviato. Il punto aggiunge al warning i **nomi** dei cookie presenti (mai i valori, R6), l'URL completo e il referer. | `app/Http/Middleware/RedirectIfAuthenticated.php` | basso | man | Si ripete il giro login -> app -> rimbalzo: il warning nel log nomina i cookie visti e l'URL di provenienza. **Applicato**: `php -l` pulito, suite **128 passed (267 assertions)** con `./scripts/run-test-backend.sh`. Il contenuto del warning si legge al prossimo rimbalzo reale. |
| TMT27 | **fatto** (2026-08-28) — **chiude `VDF14`** | L'exchange passa `canCreate: false`: **rinnova e non crea**. Si è potuto fare solo dopo `TMT28`, che sposta la creazione al login — sennò sarebbe stato `VDF16`, il primo accesso impossibile | `app/Http/Controllers/Manage/SessionController.php` | medio | auto | il login apre la sessione, l'amministratore la revoca, l'exchange col master token ancora valido risponde **403** e la riga **resta cancellata**. E un test è caduto applicandolo — quello dei tre header, che non apriva la sessione prima: è la prova che il comportamento è cambiato |
| TMT15 | **fatto** (2026-08-28) — **con una correzione al punto** | I due «no» dell'exchange erano lo stesso 403. Ora un `provider_id` che non esiste risponde **404**, non 500 come diceva questo punto: un identificativo sbagliato nella richiesta è un errore **del chiamante**, e 500 direbbe «il server è rotto» a chi ha solo scritto male un numero. Il 403 resta dov'è giusto: «il provider esiste, e tu non puoi» | `app/Http/Controllers/Manage/SessionController.php`, `lang/*.json` | medio | auto | `provider_id` inesistente → **404**; utente senza ruoli → **403**; utente con accesso → **200** |
| TMT16 | **fatto** (2026-08-28) — **forma cambiata dal developer** | La `v2` restituisce i due token **negli header**: `x-master-token` e `x-app-token`, corpo vuoto. Avevo scritto e consigliato `{"master_token", "app_token"}` nel corpo, con l'argomento che `x-` è convenzione degli header; il developer ha scelto gli header, ed è **simmetrico alla richiesta**, che i token li manda negli header a sua volta. La `v1` non è stata toccata: risponde ancora `{"token"}` nel corpo | `app/Http/Controllers/Manage/SessionController.php` | basso | auto | due test: la v2 ha i due header, il corpo **vuoto**, e `x-master-token` è quello presentato; la v1 ha `token` nel corpo e **non** ha quegli header |
| TMT17 | **fatto** (2026-08-28, ampliato il 2026-08-31) | **La rotazione, e solo sulla `v2`**: se il master token presentato è più vecchio della soglia se ne genera uno nuovo, che da lì in poi è quello che vale — lo salva la riga e lo riceve il chiamante nell'header. Il vecchio **non viene invalidato**: chi non sa leggere l'header nuovo continua a funzionare fino alla sua scadenza. La `v1` non ruota. **La soglia è un parametro** — `master-token-rotate-after-seconds` in `patemeters`, con ripiego a **un'ora** — come le due durate dei token, e per la stessa ragione: è un numero che si vorrà cambiare senza un rilascio. L'età si legge dal claim `iat` che `VerifyMasterToken` ha **già verificato** | `app/Http/Controllers/Manage/SessionController.php`, `app/Services/TokenProviderService.php`, `app/Http/Middleware/VerifyMasterToken.php` | **alto — tocca l'autenticazione di tutti i client** | auto | **otto test**. I quattro della rotazione: token di due ore → header diverso e riga aggiornata; token fresco → invariato; il vecchio funziona ancora; la v1 non ruota. E i quattro della **scadenza**, con la durata portata a **un secondo** dal parametro: sulla `v1` l'exchange va **subito** e risponde **401** dopo 1,1 s; sulla `v2` con la soglia a un'ora **succede lo stesso** — la scadenza vince, e non è scontato: «la v2 ruota quindi non scade mai» è falso; sulla `v2` con la soglia a **un secondo** il token si rinnova da sé e **il nuovo funziona**, che è il caso per cui la rotazione esiste; e il ripiego a 3600 senza riga a database |
| TMT18 | **fatto** (2026-08-28) | Ogni app token staccato **dalla v2** scrive una riga in `audits`: evento `created`, `auditable_type` `AppToken`, e nei dettagli `provider_id`, il token e `created_at`. Se l'inserimento fallisce **non blocca l'accesso**: si perde la riga, non la sessione | `app/Http/Controllers/Manage/SessionController.php` | basso | auto | dopo un exchange v2 la riga c'è con l'utente giusto; dopo un exchange **v1** non c'è |
| TMT19 | **scartato** (2026-08-28) — **decisione del developer, rischio annotato** | Proponeva l'impronta al posto del token nei dettagli dell'audit. Il developer ha riconfermato di volerci il token: si fa così. La ragione data — «è firmato» — è però rovesciata, e la nota resta nel § 6 dell'analisi perché il giorno che qualcuno chiederà «perché c'è un token dentro gli audit?» la risposta non sia «non ci avevamo pensato» | nessuno | — | — | il punto non si esegue |
| TMT20 | **scartato** (2026-08-28) | La colonna «Provider» di `admin/sessions` **resta**: con la strada (a) ogni riga ha ancora il suo provider | nessuno | — | — | non si esegue |
| TMT21 | **fatto** (2026-08-28) — **due test nuovi, non cinque** | Il punto chiedeva cinque comportamenti coperti. Tre lo erano già, scritti insieme ai punti che li hanno creati, e ripeterli sarebbe stato lavoro doppio: l'exchange che rigenera sempre e la riga che porta il master token stanno in `TokenRefreshTest`; i tre modi di mandare il master token pure, più il test unitario di `masterTokenFrom()`. **Mancavano davvero in due**, e sono quelli scritti ora: il login **all'IdP** che salva tutti e due i token (`TMT03` — senza, l'IdP sarebbe l'unico posto che non può rinnovare) e l'utente **senza ruoli** su un provider esistente che riceve **403** (`TMT15`, il caso che non va confuso col 404 del provider inesistente) | `tests/Feature/Auth/SessionRevocationTest.php` | basso | auto | **prova al contrario eseguita**: tolto `$masterToken` dalla chiamata del login, `logging into the idp saves both tokens` **fallisce**; rimesso, suite **113 passed (223 assertions)** |
| TMT22 | **fatto** (2026-08-31) — **da lanciare** | `sessions.provider_id` diventa **nullable**: senza, la riga della v2 — che rappresenta il master token e non un'applicazione — non si potrebbe scrivere. Il guadagno secondario è il marcatore: `provider_id IS NULL` distingue una riga v2 da una v1 senza aggiungere una colonna «tipo» che poi qualcuno dovrebbe tenere allineata. La chiave esterna resta e vale per le righe che un provider ce l'hanno: in SQL un valore nullo non si verifica contro la tabella di riferimento. **Il `down()` cancella le righe senza provider**, e non c'è altro modo — sono proprio quelle che la colonna non nullable non ammette | nuova migrazione | medio | auto | la suite la applica a ogni test (**124 passed**); su MariaDB risulta `Pending` e va lanciata |
| TMT23 | **fatto** (2026-08-31) | **La `v2` ha un modello suo**: una riga per utente, senza provider e senza app token, col master token in `refresh_token`. L'exchange v2 ora **esce prima** e non passa da `getValidProviderToken()`, che è il metodo delle righe per provider: la v1 resta esattamente com'era. La riga la scrive il **login** (`openProviderSession()`), quindi se manca è una revoca e la v2 risponde 403 — la stessa regola di `TMT27`, applicata all'altro modello. Serve un `whereNull`: `where("provider_id", null)` in SQL **non trova mai niente** | `app/Services/SessionService.php`, `app/Http/Controllers/Manage/SessionController.php` | **alto — è il modello nuovo** | auto | **tre test**: il login scrive la riga senza provider; un exchange v2 su un **secondo** provider **non aggiunge** righe; revocata la sessione, la v2 risponde 403. **E un buco trovato da un test che è caduto**: dopo la rotazione la riga del master token restava col token vecchio — ora l'exchange v2 la riallinea |
| TMT24 | **fatto** (2026-08-31) — **da replicare nel repo dell'estensione** | L'estensione PHP passa alla `v2`: master token in `x-master-token`, i due token letti **dagli header** della risposta, e `IDP_URL_TOKEN_EXCHANGE` a `api/v2/…` nel readme e nella guida d'integrazione. `resolveValidAppToken()` restituisce ora **un array** e non una stringa: la rotazione del master token è l'unica informazione in più che la v2 porta, e buttarla via lì significherebbe non ruotare mai. Il token ruotato finisce nel **cookie** in tutti e due i flussi, web e API | `tmp/idp-extension/` | medio — è il client di tutte le applicazioni PHP | man | `php -l` pulito; nessun riferimento a `api/v1/token` resta nell'estensione. **Non eseguibile qui**: è un altro repository, e la prova è farla girare contro l'IdP |
| TMT25 | **fatto** (2026-08-31) — **da replicare nel repo dell'estensione** | L'estensione Node, la stessa cosa: header `x-master-token` nella richiesta, `x-app-token` e `x-master-token` letti dagli header della risposta, e il valore per difetto di `IDP_URL_TOKEN_EXCHANGE` portato a `api/v2/…` in `IdpService.js`, nel README e nel `.env.test`. `resolveValidAppToken()` restituisce un oggetto `{ appToken, masterToken }`, e il master token ruotato va nel cookie nei due flussi | `tmp/idp-extension-node/` | medio | man | `node --check` pulito su tutti i file toccati; nessun `api/v1/token` rimasto. **Non eseguibile qui** per la stessa ragione |
| TMT34 | **fatto** (2026-08-31) — **da replicare nel repo dell'estensione** | **La firma di `executeMasterTokenExchange` prometteva una stringa e restituiva un array.** Difetto introdotto da `TMT24`: il metodo con la v2 torna **due** token (app token e, solo se ruotato, master token nuovo), ma il tipo di ritorno era rimasto `: string`. PHP se ne accorge solo a runtime, e a scambio gia' riuscito: `TypeError … Return value must be of type string, array returned`. Corretto in `: array` con l'annotazione della forma. `resolveValidAppToken` e i suoi due chiamanti erano gia' coerenti (verificato). | `tmp/idp-extension/src/Http/Middleware/IdpAuthMiddleware.php` | basso | man | `php -l` pulito; il giro reale supera lo scambio invece di fermarsi sul `TypeError`. |
| TMT35 | **scartato** (2026-08-31) | **`0` come marcatore di «tutti i provider» al posto di `NULL`: il tipo lo regge, il vincolo no.** Verificato: `provider_id` e' `int(10) unsigned`, quindi `0` ci sta (a differenza di `-1`, che in una colonna `unsigned` non entra, e di `"*"`, che non e' un numero). A fermarlo e' `sessions_provider_id_foreign`: un `provider_id = 0` pretende una riga in `providers` con `id = 0`, e non c'e' (`min(id) = 1`, verificato). Crearla non e' immediato: `providers.id` e' `AUTO_INCREMENT` e il `sql_mode` di questo database e' `ONLY_FULL_GROUP_BY,STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION` — **senza `NO_AUTO_VALUE_ON_ZERO`**, quindi inserire `id = 0` non scrive zero: MariaDB lo interpreta come «dammi il prossimo id» e ne assegna un altro. Restano due strade: (a) **cancellare il vincolo**, e allora si perde l'integrita' verso `providers` e il `ON DELETE CASCADE` che oggi ripulisce le sessioni quando un provider viene cancellato; (b) **creare davvero la riga 0** cambiando il `sql_mode` per quella sola istruzione — la riga poi esiste, quindi comparirebbe negli elenchi dei provider, nell'assegnazione dei ruoli e in `hasAccessToProvider`, e andrebbe esclusa a mano in ognuno. **Consiglio: tenere `NULL` nel database — che e' gia' il modo in cui SQL dice «nessun provider», costa zero migrazioni e conserva il vincolo — e fare il `"*"` dove serve davvero, cioe' nell'elenco: e' quello che fa `TMT38`.** Se approvi comunque il marcatore `0`, va detto quale delle due strade. | `database/migrations/`, `app/Services/SessionService.php`, `app/Http/Controllers/Manage/SessionController.php` | **alto** — breaking nella strada (a): senza vincolo nulla impedisce piu' una sessione verso un provider inesistente, e cancellando un provider le sue sessioni **restano** invece di sparire; breaking nella strada (b): la riga 0 e' un provider a tutti gli effetti e ogni elenco che non la escluda la mostra agli amministratori. In entrambe, ogni `whereNull("provider_id")` gia' scritto (`SessionService::masterSessionFor`) va cambiato insieme, e una sola dimenticanza lascia sessioni che nessuno trova piu' | man | `SHOW CREATE TABLE sessions` e `SELECT id FROM providers WHERE id = 0` mostrano lo stato scelto; login e scambio v2 continuano a trovare la riga comune; suite verde. **Deciso dal developer**: marcatore **`0`**, e **niente `varchar`** — la colonna resta intera. Resta da scegliere **come** far accettare lo zero al vincolo: strada (a) cancellarlo, strada (b) creare davvero la riga `providers.id = 0`. Non si implementa prima di quella risposta, perche' le due strade lasciano il database in due stati diversi e nessuna delle due si torna indietro con un `git checkout`. Nota: dopo `TMT38` il `*` si vede gia' nell'elenco senza toccare il database, quindi questo punto non serve piu' a mostrarlo — serve solo se si vuole il marcatore **dentro** i dati. **Scartato dal developer**: dopo `TMT38` il `*` si vede gia' nell'elenco senza toccare il database, quindi il marcatore dentro i dati non serve piu' a niente che non sia gia' risolto — e costerebbe o il vincolo verso `providers` o una riga finta fra i provider. `NULL` resta il modo in cui il database dice «nessun provider». |
| TMT36 | **sospeso** (2026-08-31) | **Una riga sola per tutte le app, IdP compreso.** Oggi no: l'IdP si autentica sulla **sua** riga. `RedirectIfAuthenticated::resolveAuthenticatedUser` accetta l'utente solo se `Session::where("token", $tokenString)->exists()` (`RedirectIfAuthenticated.php:141`), e `IdpSessionValidator::isActive()` fa lo stesso; quel `token` e' l'app token dell'IdP, che sta nella riga `provider_id = 1`. La riga senza provider ha `token = ""` (`SessionService::upsertMasterSession`), quindi con quelle due ricerche non verrebbe mai trovata. Per arrivare a **una riga sola** il riconoscimento deve smettere di cercare **per token** e cercare **per utente piu' validita'**: si verifica la firma del token (che gia' si verifica), si legge `sub`, e si controlla che esista la riga senza provider non scaduta — piu' `hasAccessToProvider`, che resta il punto in cui i permessi decidono. Il punto tocca `resolveAuthenticatedUser`, `IdpSessionValidator`, `IdpTokenRenewer` e `CustomAuditable::getActor` (che cerca `where("provider_id", $actor["providerId"])`). | `app/Http/Middleware/RedirectIfAuthenticated.php`, `app/Auth/Idp/IdpSessionValidator.php`, `app/Auth/Idp/IdpTokenRenewer.php`, `app/Traits/CustomAuditable.php`, `app/Services/SessionService.php` | **alto** — breaking: **si perde la revoca per singola applicazione**. Oggi cancellare la riga di un provider chiude l'accesso a quella sola app (e' il motivo per cui `VDF14` e' stato chiuso cosi'); con una riga sola l'unica revoca possibile e' «tutto o niente». Inoltre l'audit perde il `provider_id` da cui l'azione proviene, e ogni app che oggi legge la propria riga per sapere se la sessione e' viva vedrebbe la riga comune. **Da decidere prima di approvare**: la revoca per singola app la si vuole ancora? Se si', i due modelli non stanno insieme. | man | Login sull'IdP con la sola riga senza provider: l'utente entra e naviga; un utente senza ruoli sul provider IdP resta fuori; la revoca della riga comune fa cadere IdP e app insieme; suite verde. **Sospeso per decisione del developer**: `TMT35` e `TMT38` bastano per quello che serviva adesso — vedere e trattare la riga comune — mentre questo punto porta breaking change veri (la revoca per singola applicazione, l'audit senza `provider_id`) per un guadagno che al momento non e' richiesto. Resta scritto perche' e' la strada da riprendere se un giorno si vuole davvero **una** riga per utente: qui c'e' gia' l'elenco di cio' che andrebbe toccato. |
| TMT37 | **fatto** (2026-08-31) | **Quando l'utente ha piu' righe e una e' quella comune, si mostra solo quella.** `SessionController@all` (`SessionController.php:30`) elenca **tutte** le righe: dopo `TMT32` un utente entrato in due applicazioni ne ha tre, e la riga senza provider compare accanto alle altre con il provider vuoto. Il punto filtra l'elenco: se per un `user_id` esiste la riga senza provider, si mostra quella e si nascondono le sue righe per provider. Si fa con una sola query — `whereNull("provider_id")` in `or` con «non esiste una riga senza provider per lo stesso utente» — e **non** in PHP dopo il `paginate`, sennò le pagine escono di dimensioni diverse. Va deciso anche cosa mostra la colonna del provider per quella riga: oggi sarebbe vuota, e «vuoto» si legge come un errore. | `app/Http/Controllers/Manage/SessionController.php`, `resources/js/Pages/` (la vista dell'elenco) | **medio** — breaking per chi guarda: le righe per provider **spariscono dall'elenco**, quindi dall'interfaccia non si possono piu' revocare una per una anche se nel database esistono ancora; e la revoca fatta sulla riga comune ha un effetto diverso da quella su una riga per provider. Se `TMT36` passa il problema si scioglie da solo, perche' le righe per provider non esistono piu'; se non passa, i due modi di revocare convivono e l'elenco ne mostra uno solo | man | Utente con riga comune piu' due righe per provider: l'elenco ne mostra **una**; utente senza riga comune: si vedono le sue righe per provider; il conteggio della paginazione corrisponde alle righe mostrate. **Applicato**. **Record e riga non sono la stessa cosa, e qui la differenza e' tutto**: i record per provider **restano nel database** — questo punto toglie solo le loro **righe dalla tabella** che l'amministratore guarda, perche' ripetono quello che dice gia' la riga comune. Il filtro e' un `whereNull("provider_id") OR NOT EXISTS(<riga comune dello stesso utente>)`, dentro la query e prima del `paginate`. Misurato sul database reale: **3 record, 1 riga**. Copertura nuova in `tests/Feature/SessionListTest.php`, sei test: la riga comune nasconde le altre; senza riga comune le righe per provider restano; la riga comune di un utente non tocca quelle di un altro; l'ordinamento per provider non fa sparire la riga comune; l'elenco non porta mai `token` ne' `refresh_token`; il tetto a `per_page` tiene. Suite **134 passed (292 assertions)**. |
| TMT38 | **fatto** (2026-08-31) | **Nell'elenco delle sessioni la riga comune sparisce se si ordina per provider, e va invece mostrata come `*`.** Difetto reale, trovato guardando la `join` che hai indicato: `SessionController@all` ordina con `->join("providers", "sessions.provider_id", "=", "providers.id")` (`SessionController.php:59-63`), che e' una **inner join**: le righe senza provider non hanno corrispondenza e quindi **spariscono dall'elenco** appena l'amministratore clicca la colonna del provider. Non e' un caso di scuola: dopo `TMT32` ogni utente entrato ha una riga comune, e ordinando per provider non ne vede piu' nessuna. Il punto fa due cose: la join diventa `leftJoin`, cosi' la riga resta; e il nome del provider per quella riga diventa `*` invece di vuoto — con `COALESCE(providers.name, '*')`, ordinando su quello. Il `*` cosi' si vede nell'elenco **senza toccare il database**, che e' il punto in cui serviva davvero. Vale sia se resta `NULL` sia se passa `TMT35`: con `0` e nessuna riga `providers` corrispondente la inner join le nasconderebbe uguale. | `app/Http/Controllers/Manage/SessionController.php`, `resources/js/Pages/` (intestazione della colonna) | basso — nessun breaking: l'elenco mostra **piu'** righe di prima, non meno; cambia solo l'ordinamento relativo delle righe comuni, che finiscono dove `*` cade nell'ordine alfabetico | man | Con un utente che ha la riga comune: si ordina per provider in salita e in discesa e la riga resta visibile in entrambe, con `*` nella colonna del provider; il totale della paginazione non cambia rispetto all'ordinamento predefinito. **Applicato e misurato**: sullo stesso database, `INNER JOIN` contava **2** righe e `LEFT JOIN` ne conta **3** — la riga comune era davvero sparita. L'elenco ordinato per provider ora rende `*`, `device`, `IDP` in quest'ordine. Nel passaggio e' caduto anche un `sessions.*` che nei due rami di ordinamento portava fuori `token` e `refresh_token`: le colonne ora sono nominate una per una (verificato: `token_esposto=no` su tutte le righe). Aggiunto un tetto a `per_page` (max 100): con `per_page=999999` la pagina resta di 100. Suite **128 passed (267 assertions)**. |
| TMT39 | **fatto** (2026-08-31) | **I test della scadenza gareggiavano con l'orologio, e ogni tanto perdevano.** `TMT17` li aveva scritti con una durata di **un secondo**: il token nasceva, si apriva la sessione a database, e solo dopo partiva la prima chiamata, che doveva arrivare entro quel secondo. Su una macchina carica non ci arrivava — osservato: `Expected response status code [200] but received 401` in `test_on_v1_an_expired_master_token_cannot_renew_anymore`. Il difetto era nel test, non nel codice. Ora i token si firmano a mano con `iat` ed `exp` **scelti**: scaduto o vivo, sempre, senza attese. Spariti tutti e tre gli `usleep`. La durata come parametro non e' scoperta: la copre un test nuovo che legge `exp - iat` dentro il token. | `tests/Feature/Auth/SessionRevocationTest.php` | basso | auto | Suite **135 passed (293 assertions)**, e **cinque esecuzioni di fila** tutte verdi; nessun `usleep` resta nel file (`grep` vuoto). |
## Perf/leak — la dichiarazione della policy per `TMT01`…`TMT05`, `TMT07`…`TMT10`

Policy dell'organizzazione, voce per voce. I punti chiusi toccano **un service** (`SessionService`), **due middleware** (`VerifyMasterToken` e `Authenticated`),
due **controller**, una **classe nuova** (`IdpTokenRenewer`) e le rotte: è il caso in cui la checklist serve davvero.

| Voce | Esito | Perché |
|---|---|---|
| Query N+1 | **verificato** | nessuna query aggiunta. `getValidProviderToken()` ne fa **una in meno per giro**: il ramo tolto da `TMT01` faceva un `first()` per decidere se riusare il token, e ora si va dritti a `upsertSession()`, che quel `first()` lo fa comunque |
| Data leakage | **verificato applicabile, e pulito** | i messaggi di `TMT09` dicono «riprova ad accedere» e **non** perché: che la sessione sia stata revocata dall'amministratore o che l'utente abbia perso i ruoli è nel log del server, non sullo schermo. I log **escono dalla macchina** (`LOG_SERVICE_URL` in `.env`), e un JWT firmato e non scaduto è una credenziale al portatore. Nessun token viene scritto in chiaro: passano tutti da `SessionService::tokenFingerprint()`, che dà le ultime otto lettere e la lunghezza. Verificato con un `grep` su `app/`: **nessun** `Log::` riceve una variabile di token che non sia l'impronta |
| Scope/tenant | **verificato** | `getValidProviderToken()` continua a filtrare per `user_id` **e** `provider_id`, e la prima cosa che fa resta `hasAccessToProvider()`. Il master token si scrive nella riga di quell'utente, non si cerca |
| Memory/streaming | non applicabile | stringhe e una riga per volta |
| Query non vincolate | **verificato** | il rinnovo aggiunge **due** query per pagina scaduta — un `first()` sulla riga di sessione e l'upsert — e succede una volta ogni trenta minuti per utente, non a ogni richiesta. Nessun `get()` nuovo. `refresh_token` non è ancora interrogato da nessuno — lo sarà con la `v2`, e lì la mancanza di indice è una scelta già presa e scritta (`D1`) |

## Un difetto trovato verificando `TMT05`, e corretto: la cache delle rotte

`route:list` non mostrava la rotta nuova. Non era la rotta: **`bootstrap/cache/routes-v7.php` esisteva,
datato 31 luglio**, e Laravel legge quello invece dei file. Da cui due fatti, il secondo peggiore:

1. nell'applicazione la `v2` risponderebbe **404** finche' qualcuno non svuota quella cache — e il file
   e' di `www-data`, quindi lo svuota il container, non il developer;
2. **la suite dei test caricava la stessa cache** (verificato: `getCachedRoutesPath()` esiste e punta
   la'). Cioe' i test sulle rotte provavano le rotte del **31 luglio**, e una rotta aggiunta dopo non
   esisteva per loro. Nessuno poteva accorgersene: i test passavano.

E' il gemello di `VDF11` — la config cache che rende `env()` inerte — nell'altra cache. **Corretto nello
stesso modo**: `scripts/run-test-backend.sh` devia ora anche `APP_ROUTES_CACHE` fuori dall'albero, come
gia' faceva con `APP_CONFIG_CACHE`. Misurato prima e dopo: con la deviazione la suite resta **96
passed**, e `route:list` passa da **una** rotta di exchange a **due**.
