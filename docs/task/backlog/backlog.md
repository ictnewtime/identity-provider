# Dubbi e proposte non ancora decise

**Identificatori**: `BDB` = registro dei dubbi

Aggiornato il 2026-08-20 (`date +%F`). **`BDB36` non compare**: era la nota del task sui return multipli, ritirata insieme al task il 2026-08-20 — l'ID resta speso. **Non si contano e non scadono**: una proposta che sta ferma
sei mesi non diventa urgente, e nemmeno sbagliata.

Ogni voce porta **come si scioglie** — il comando, il file da guardare o la persona che decide.
Un dubbio senza modo di scioglierlo resta un dubbio per sempre, e questa lista serve a te per quando
hai tempo, non a me per averlo scritto.

**Le domande già dentro un task non si copiano qui.** Le sezioni § 4 delle quattro analisi in
`todo/` sono la loro sede: duplicarle darebbe due verità che divergono, e la seconda divergenza non
la nota nessuno. Qui sta ciò che **nessun piano copre** — le voci che i quattro piani hanno
esplicitamente lasciato fuori, e i buchi della procedura.

## Da decidere, con conseguenze sul codice

| ID          | Dubbio                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Come si scioglie                                                                                                                                                                             |
| ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ~~`BDB01`~~ | **Quale strumento ha prodotto i rilievi.** — **Sciolto il 2026-08-12**: è **SonarQube**, in pipeline, con `-Dsonar.qualitygate.wait=true` (`.github/workflows/deploy-staging.yml:38` e l'omologo di produzione). Quindi il cancello **blocca già oggi** i deploy, e i rilievi delle tranche `v1`–`v4` non sono un parere: fermano un rilascio. Restano da trovare le regole attive e il modo di sopprimere un falso positivo (`TSA06`)                                                                                  | ✅ risolto guardando i workflow. Il seguito è in `BDB17`                                                                                                                                     |
| `BDB02`     | **Nessun rilievo si riproduce in locale — ne' JS ne' PHP.** `D5` ha escluso ESLint (`TSA09` scartato) e SonarQube gira solo in pipeline: chi scrive il codice scopre un rilievo **dal quality gate**, cioe' al momento del rilascio. E' il costo accettato con `D5`, ed e' scritto qui perche' sia una scelta e non una sorpresa                                                                                                                                                                                        | decidere fra PHPStan/Larastan nel repo, uno scanner Sonar locale, o niente. Verifica: un comando che rieseguito trova gli stessi rilievi                                                     |
| ~~`BDB17`~~ | **`-Dsonar.sources=.`** analizza la radice intera. — **Scartato dal developer il 2026-08-19.** Metà della voce si è sciolta da sé: i rilievi da `cypress/e2e-bk/` non arrivano più perché **la cartella è stata cancellata**. L'altra metà — `vendor/` e `node_modules/` che finiscono nell'analisi — **resta vera e non verificata**: nessuno ha guardato il numero di file nel report, e il quality gate può ancora fermarsi su codice di terzi. Sta qui, scartata, perché sparire non è lo stesso che essere risolta | ✅ scartato dal developer                                                                                                                                                                    |
| `BDB18`     | **Nessuno smoke test automatico dopo il deploy**: la §2 di `docs/post-deploy.md` è una checklist che esegue una persona. È **qui** che il problema vive davvero — la macchina finale ha un IP e non un nome di dominio, e il container che costruisce l'immagine non la raggiunge. I test di `TSD` girano prima e altrove, e non lo risolvono                                                                                                                                                                           | decidere chi esegue lo smoke: un task Ansible dopo il deploy, un monitoraggio esterno, o niente. Verifica: un deploy con Swagger rotto viene segnalato senza che nessuno apra una pagina     |
| `BDB19`     | **`storage` non è persistito** (nessun volume nel deploy Ansible): le chiavi Passport e quelle RS256 del master token si rigenerano a ogni release, e il JWKS del master token **cambia** — i provider esterni che lo cachano falliscono la verifica. È il vero motivo della §1 obbligatoria del post-deploy. `TSD06` mette al riparo solo il JSON di Swagger                                                                                                                                                           | `docs/post-deploy.md` §4 propone già le due soluzioni. Verifica: due deploy consecutivi lasciano invariata `storage/app/keys/public.key`                                                     |
| `BDB03`     | **API Resource per gli audit.** Oggi la risposta restituisce i modelli interi, `user` compreso (`AuditController.php:71-79`). Non è un leak attuale — la legge un amministratore — ma è la forma che lo rende possibile al primo ruolo nuovo. Introdurla **rompe la tabella Vue**                                                                                                                                                                                                                                       | task suo, da fare col frontend insieme. Verifica: `app/Http/Resources/` contiene la risorsa e il controller non restituisce più modelli nudi                                                 |
| `BDB04`     | **Gli audit hanno un tenant?** La rotta `routes/web.php:144` non filtra per provider né organizzazione. Se un amministratore è globale per definizione va **scritto**, non lasciato implicito nell'assenza di un `where`                                                                                                                                                                                                                                                                                                | lo decidi tu. Verifica: o un `where` di scope, o una riga nei requisiti che dichiara la scelta                                                                                               |
| `BDB05`     | **Ordinare su una relazione polimorfa.** `TCC03` toglie `user.username` dalle colonne ordinabili: ferma il difetto `VDF02`, non lo ripara. La soluzione vera deve ordinare su due tabelle diverse (`users.username` e il nome del client Passport) e non ne ho una buona                                                                                                                                                                                                                                                | progettazione. Verifica: la colonna torna ordinabile e un test copre entrambi i tipi di attore                                                                                               |
| `BDB06`     | **I percorsi delle rotte sono dichiarati due volte** — in `routes/web.php` e nelle annotazioni `OA\*`. Rinominare una rotta lascia la documentazione che punta alla vecchia, e niente se ne accorge. I rilievi sui literali duplicati hanno sfiorato questa duplicazione senza nominarla                                                                                                                                                                                                                                | un controllo che verifichi che ogni percorso annotato corrisponda a una rotta esistente. Verifica: lo script fallisce se si rinomina una rotta senza toccare l'annotazione                   |
| `BDB07`     | **`withTrashed()` incoerente** in `ProviderUserRoleController`: `find()` (`:219`) trova i record cancellati logicamente, `update()` (`:290`) e `delete()` (`:331`) no. Può essere voluto — si consulta un cancellato ma non lo si modifica — o una svista                                                                                                                                                                                                                                                               | lo sai tu. Verifica: se voluto, un commento che lo dice; se no, diventa un difetto e apre un punto                                                                                           |
| `BDB08`     | **Costruttore vuoto in `app/Services/AccountService.php:27`**, gemello di quello di `TPU02`. Non l'ho messo in nessun piano: toccare un service fa scattare il controllo perf/leak completo, che per cancellare una riga non si giustifica                                                                                                                                                                                                                                                                              | agganciarlo al prossimo task che tocca quel service per motivi veri                                                                                                                          |
| ~~`BDB09`~~ | **Riscrivere la storia git** per `cypress.env.json`. — **Chiuso il 2026-08-12** (`D1`): non si fa. Le credenziali erano dummy e usate solo in locale, quindi non c'è niente nel passato da ripulire                                                                                                                                                                                                                                                                                                                     | ✅ deciso dal developer                                                                                                                                                                      |
| ~~`BDB10`~~ | **`cypress/e2e-bk/`** — cancellare o tenere. — **Deciso il 2026-08-12** (`D3`): **si tiene**, per ora. `TSA07` è scartato e `TSA08` si attiva. Il seguito è escluderla lato SonarQube, che è `BDB17`                                                                                                                                                                                                                                                                                                                    | ✅ deciso dal developer                                                                                                                                                                      |
| `BDB20`     | **Credenziali E2E contro un ambiente non effimero.** `TSA10`–`TSA13` le generano a ogni esecuzione: funziona perché gli utenti si possono ricreare. Se un giorno i test E2E dovessero girare contro un ambiente stabile — dove non si semina — servirebbe un segreto di lunga durata, e la sede naturale è **Infisical**, che la pipeline già usa (`ansible/builder/tasks/identity-provider.yml:3-8`)                                                                                                                   | si scioglie quando si decide dove girano i test E2E in pipeline. Verifica: se l'ambiente è effimero, questa voce si chiude senza fare niente                                                 |
| `BDB21`     | **`cypress.env.example.json` (`TSA01`) e `scripts/prepare-e2e-credentials.sh` (`TSA10`) sono due elenchi delle stesse chiavi.** Aggiungerne una in un posto solo passa inosservato: il file di esempio è pensato per essere letto, lo script per essere eseguito, e il secondo vince in silenzio                                                                                                                                                                                                                        | un controllo che confronti le chiavi dei due, o l'eliminazione del file di esempio a favore del solo script. Verifica: aggiungendo una chiave a uno dei due, qualcosa fallisce               |
| `BDB22`     | **L'immagine di produzione porta il binario di Cypress, dove non puo' funzionare.** `Dockerfile:49` esegue `npm install` senza distinguere le dipendenze di sviluppo: scarica ~1,5 GB di cache Cypress (`du -sh ~/.cache/Cypress`) in un'immagine a cui mancano **tutte** le librerie grafiche che gli servono (`docs/TEST.md`). Peso e superficie in piu', utilita' zero                                                                                                                                               | `npm ci --omit=dev` nel `Dockerfile`, o `CYPRESS_INSTALL_BINARY=0`. Verifica: la dimensione dell'immagine prima e dopo                                                                       |
| `BDB23`     | **Il `Dockerfile` non fissa la versione di Node.** Arriva da `apt` della base `php:8.2-fpm`, oggi Debian trixie con `nodejs` 20.19 — che soddisfa Cypress 15 (`engines: ^20.1.0 \|\| ^22.0.0 \|\| >=24.0.0`) **per caso**. Un aggiornamento della base puo' spostarla in su o in giu' senza che nessuno lo decida                                                                                                                                                                                                       | fissare la versione (NodeSource, o `FROM node` in uno stage). Verifica: `node -v` nell'immagine non cambia quando cambia la base                                                             |
| `BDB24`     | **Nessun runner di test JS.** `D5` ha escluso ESLint e il progetto non ha ne' Vitest ne' Jest ne' uno script `test` in `package.json`: una correzione nel frontend **non si puo' provare**, si puo' solo guardare. E' il gradino sotto `BDB02` — li' i rilievi non si riproducono, qui non si verifica nemmeno che una correzione funzioni. Ha gia' cambiato la verifica di un punto (`TSA04`)                                                                                                                          | decidere se Vitest vale la dipendenza. Verifica: un `npm test` che fallisce su un caso reintrodotto di proposito                                                                             |
| `BDB25`     | **`npm run build` non e' eseguibile in locale**: `lang/php_en.json` e' di proprieta' di `root` (`-rw-r--r-- root root`, generato dal container) e il plugin `laravel-vue-i18n` deve riscriverlo → `EACCES`. Chi sviluppa sull'host non puo' costruire il frontend, e nessuna verifica che passi dal build e' disponibile                                                                                                                                                                                                | i file generati dal container dovrebbero appartenere all'utente. Verifica: `npm run build` completa senza `EACCES`                                                                           |
| ~~`BDB26`~~ | **La config cache rende i test ciechi a `phpunit.xml`.** — **Promosso a difetto il 2026-08-12**: non era piu' un dubbio, si e' realizzato. Ha svuotato `idp_staging`. E' `VDF11`, con correzione in `TCC15` + `TCC14`                                                                                                                                                                                                                                                                                                   | ✅ diventato un difetto con un punto di piano                                                                                                                                                |
| `BDB27`     | **`UserFactory` non valorizza `enabled`**, che nello schema e' NOT NULL: `User::factory()->create()` fallisce con un vincolo di integrita'. Ogni test che crea un utente deve passarlo a mano, e chi non lo sa perde tempo sul messaggio sbagliato                                                                                                                                                                                                                                                                      | una riga nella factory. Verifica: `User::factory()->create()` senza argomenti non solleva eccezioni                                                                                          |
| `BDB28`     | **`MYSQL_DATABASE` e `MYSQL_USER` del servizio MariaDB sono inerti.** L'immagine li legge **solo alla prima inizializzazione del volume**: `mariadb_data` esiste gia', quindi cambiare `MYSQL_DATABASE: idp_develop` **non creera' quel database**. In piu' `MYSQL_USER: root` non e' supportato dall'immagine — l'utente `root` esiste gia' e la variabile viene ignorata o rifiutata. Chi legge il compose crede che il database nasca da se'                                                                         | rimuovere `MYSQL_USER` e non contare su `MYSQL_DATABASE`: la creazione sta in `docs/setup.db.md`. Verifica: `docker volume rm mariadb_data` e riavvio ricreano l'ambiente descritto dal file |
| ~~`BDB29`~~ | **`DatabaseSeeder` non e' idempotente.** — **Spostato il 2026-08-12** in [local-environments](../done/20260812-local-environments/action-plan.md) come `TLE10`: e' una questione di ambienti, non un dubbio sul modo di lavorare. Qui non resta niente                                                                                                                                                                                                                                                                  | ✅ diventato un punto di piano                                                                                                                                                               |
| `BDB30`     | **La tabella dei parametri si chiama `patemeters`** — un refuso, per `parameters`. Sta nella migrazione (`database/migrations/2026_03_23_102105_patemeters.php:13,28`, piu' il nome del file) e nel modello, che lo aggira con `protected $table = "patemeters"` (`app/Models/Parameter.php:14`). Funziona, ma ogni query scritta a mano contro il database va sbagliata al primo tentativo, e chi legge lo schema non capisce                                                                                          | rinominare la tabella con una migrazione, non modificare quella vecchia. Verifica: `show tables` elenca `parameters` e il modello non dichiara piu' `$table`                                 |
| `BDB11`     | **Il messaggio `"Role id not found"`** (`app/Http/Controllers/`) è lo stesso messaggio degli altri undici scritto male. Entra in `TPU06` solo se decidi di estendere la correzione a tutto lo strato                                                                                                                                                                                                                                                                                                                    | ricade su `D1` di `v4`                                                                                                                                                                       |

## Buchi della procedura, non del prodotto

Emersi lavorando, e senza casella: `docs/ai/full/` non esiste in questo repo, quindi `full/backlog.md`
— dove R7 manderebbe queste righe — non c'è. Restano qui finché non c'è.

| ID          | Dubbio                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Come si scioglie                                                                                                                                                                                        |
| ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `BDB12`     | **`docs/todo-manual.md` non esiste.** Serviva a `TSA02`, ora scartato — quindi al momento nessun punto e' senza casella. Il buco resta: il primo intervento fuori dal repo non avra' dove andare. `TSA14` (`git rm --cached`, che esegue il developer) e' il candidato piu' prossimo                                                                                                                                                                                                                                                                                                                                                                                                                  | crearlo, o dichiarare che in questo progetto gli interventi manuali stanno nei piani                                                                                                                    |
| `BDB13`     | **`.claude/rules/` non esiste**, ma `docs/ai/index.md` lo dà per presente (R8): le 17 regole vivono solo come prosa nell'indice e in `CLAUDE.md`. Nessuna è applicata da un meccanismo                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | decidere se le regole diventano file in `.claude/rules/` o restano documentazione                                                                                                                       |
| `BDB14`     | **Il cancello perf/leak non blocca.** `scripts/check-perf-gate.sh` scrive lo stato in `.claude/.perf-gate` e presuppone hook Claude Code non configurati qui: la policy dell'organizzazione che dice «nessuna eccezione» oggi è affidata alla sola disciplina                                                                                                                                                                                                                                                                                                                                                                                                                                         | configurare gli hook, oppure dichiarare esplicitamente che il controllo è manuale — la via peggiore è lasciare uno script che sembra presidiare e non presidia                                          |
| `BDB15`     | **`./scripts/check-links.sh` segnala 28 link rotti**, tutti in `docs/ai/abstract/` e tutti verso l'export mancante (`full/`, `dev-guide/`, `*-custom.md`). Finché restano, il controllo è rumore: nessuno lo leggerà per trovare il ventinovesimo, che sarà vero                                                                                                                                                                                                                                                                                                                                                                                                                                      | o si importano le parti mancanti, o si potano i link, o il controllo esclude `docs/ai/` dichiarandolo                                                                                                   |
| `BDB16`     | **I gemelli `*-custom.md` mancano tutti** — `testing-custom`, `perf-leak-custom`, `requirements-custom`. Sono i file che traducono le regole generiche su **questo** progetto, e `perf-leak.md` dice espressamente che vanno letti **prima** della checklist                                                                                                                                                                                                                                                                                                                                                                                                                                          | scriverli, a partire da `perf-leak-custom.md`, che è quello che serve a ogni modifica di service                                                                                                        |
| `BDB31`     | **`EncryptCookies` interroga il database a ogni richiesta**: il costruttore fa `Provider::pluck("id")` per costruire l'elenco dei cookie da non cifrare (`app/Http/Middleware/EncryptCookies.php:23`), e quel middleware sta su **tutte** le richieste web. Non rompe niente — è una query per richiesta che potrebbe essere una cache                                                                                                                                                                                                                                                                                                                                                                | misurare quante query fa una pagina qualunque con `DB::listen`, prima di decidere se vale una cache                                                                                                     |
| `BDB32`     | **`npm run build` non gira in locale** — **causa trovata il 2026-08-20: il container dell'applicazione gira come root** (`docker exec idp_app_2 id` → `uid=0`), ed e' li' che girano `composer install` e `npm`. I container dei **test** non c'entrano piu': `TSR01` li ha messi sull'utente dell'host e `TSR09` ha fatto in modo che non scrivano affatto nell'albero. **I 4208 file di root non ci sono piu'**: il developer li ha ripresi il 2026-08-20 con un `chown` da un container (`TSR14`, verificato: zero file di root nei tre alberi), e `lang/php_en.json` l'ha cancellato — era generato e stava in git; il 2026-08-21 la regola `/lang/php_*.json` e' entrata in `.gitignore` (`TSR16`), quindi la decisione e' **completa** nei due sensi: fuori da git e fuori dal rumore di `git status`. Quel che resta non e' il sedimento ma le **sorgenti**: chi lancia i comandi dentro il container (`TSR12`), vite avviato come root (`TSR13`) e il primo `install` su un clone nuovo (`TSR15`) di [security-reliability](./../todo/20260820-security-reliability/action-plan.md). Il testo originale: : il plugin `laravel-vue-i18n` rigenera `lang/php_en.json` all'avvio del build, e quel file è di **root** (`-rw-r--r-- root root`, scritto da un container) — `EACCES: permission denied`. Il file è anche **tracciato da git** pur essendo generato, e non è in `.gitignore`. Non è un difetto del prodotto: è l'ambiente locale che impedisce di compilare il frontend, e in CI su un checkout nuovo non si presenta                                                                                                                                                                                                                                          | provare `docker exec` come root per cambiarne il proprietario, e decidere se un file generato debba stare in git: se no, `.gitignore` più `git rm --cached`. Verifica: `npm run build` arriva alla fine |
| ~~`BDB33`~~ | **Il controllo sulle chiavi di traduzione.** — **Deciso il 2026-08-19: si rimette, senza la lista di eccezioni.** Il dubbio del developer era sul `DEBITO`, che chiedeva a una persona di ricordarsi di aggiornarlo, ed era fondato: una lista di eccezioni con la porta aperta e' una porta. Il controllo invece resta, coi fatti che lo giustificano — al primo giro trovo' **5** chiavi mancanti, due visibili **sul form di login**, e quelle due erano gia' state corrette il 14 e **tornate rotte il 19** col revert, viste solo da lui. Ora la regola e' secca: chiave senza traduzione, rosso. Resta fuori `resources/js/` (`VDF23`, 329 chiavi, 2 mancanti)                                  | ✅ deciso dal developer                                                                                                                                                                                 |
| ~~`BDB34`~~ | **Gli identificatori dei test in italiano, e la regola dice inglese.** — **Portata in un task il 2026-08-19**: è il punto `TTC03` di [translation-coverage](./../done/20260819-translation-coverage/action-plan.md). Misurato prima di spostarla: **15 file, 82 nomi di test, 3 metodi d'appoggio**. Qui resta l'ID, perché un ID non si riusa                                                                                                                                                                                                                                                                                                                                                        | ✅ portata in `TTC03`                                                                                                                                                                                   |
| `BDB35`     | **Nessuna operazione da `artisan` produce audit**, ed è una scelta che non sta scritta dove si guarda. `CustomAuditable::logAudit()` esce alla prima riga se `app()->runningInConsole()`: seeder, migrazioni, comandi di manutenzione e `tinker` non lasciano traccia. Probabilmente voluto — evita il rumore delle migrazioni — ma chi apre la tabella degli audit per ricostruire una modifica non ha modo di saperlo. Dimostrato da un test (`TAC01`, «in console non scrive nessun audit») e spiegato in un commento del codice; **non** in `project-analysis.md`. Era `D5` di [audit-complexity](./../done/20260819-audit-complexity/analysis.md), rimasta senza risposta alla chiusura del task | decidere se è voluto: se sì, tre righe in `project-analysis.md` accanto alle altre scelte di struttura; se no, è un buco nella traccia e diventa un difetto                                             |
| `BDB37`     | **Tre etichette del login non passano dalle traduzioni**: `Pages/Auth/Login.vue:89` «Username», `:115` «Password» e `Pages/Auth/ForgotPassword.vue:54` «Username» sono testo scritto nel template — le uniche tre in tutto `resources/js/`, tutte sulle pagine che un utente vede **prima** di autenticarsi. Non le vede nessun controllo: `TranslationKeysTest` verifica che ogni chiave **usata** abbia una traduzione, e qui non c'è chiave. Le due parole sono uguali in italiano e in inglese, quindi oggi non si nota; il giorno che si aggiunge una lingua, sì. Trovate il 2026-08-20 mentre verificavo `TSR03` | tre chiavi in `lang/it.json` e `lang/en.json` e tre `$t()`, oppure la decisione scritta che quelle due parole restano così. Se si correggono, il controllo che le ha trovate vale come verifica: uno script che cerca un `<label>` col testo dentro |
